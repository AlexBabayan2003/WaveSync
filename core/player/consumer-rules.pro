# Consumer rules for :core:player.
#
# Intentionally empty: media3 ships its own keep rules inside each AAR (proguard.txt), and
# PlaybackService is kept automatically because it is declared in the merged manifest.
#
# Add rules here only if this module starts using reflection of its own.
